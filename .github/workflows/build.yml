name: build
on:
  - push
  - pull_request
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: DeLaGuardo/setup-clojure@10.3
        with:
          cli: 1.11.1.1273
      - name: Run tests
        run: clojure -M:test
